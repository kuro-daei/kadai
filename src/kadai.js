// 座標 (x,y) の点が 半径 r の円の中に入っているかを判定します。
// 円に入っていれば isIn = true,
// 入ってなければ isIn = false
// としてください。
function isInCircle(x, y, r) {
  console.log(`x:${x},y:${y},r:${r}`);
  let isIn = false;
  // ここから修正
  // ここまで修正
  return isIn;
}

// 円の中に入った点の数 n と全体の点の数 totalから円周率を求めます。
// 変数 pai に求めた円周率を入れます。
function calcPai(n, total) {
  console.log(`n:${n}, total:${total}`);
  let pai = 0;
  // ここから修正
  // ここまで修正
  return pai;
}

export { isInCircle, calcPai };
