// input は自然数です。
// input が素数ならば is = true を
// 素数じゃないならば is = false を代入してください。
export default function isPrimeNumber(input) {
  console.log(`input:${input}`);
  let is = false;
  // ここから修正
  // ここまで修正
  return is;
}
