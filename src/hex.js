// 10進数 から 16進数 に変換します。
// inputもoutputも文字列にしてください。
function hex(input) {
  console.log(`10進から16進に変換, input:${input}`);
  let output = "";
  // ここから修正
  // ここまで修正
  return output;
}

// 16進数 から 10進数 に変換します。
// inputもoutputも文字列にしてください。
function dec(input) {
  console.log(`16進から10進に変換, input:${input}`);
  let output = "";
  // ここから修正
  // ここまで修正
  return output;
}

// mode が "hex" の時に 10進数 から 16進数 に変換、
// mode が "dec" の時に 16進数 から 10進数 に変換します。
// inputもoutputも文字列にしてください。
// 関数を使います。
function conv(input, mode) {
  console.log(`mode:${mode}, input:${input}`);
  let output = "";
  // ここから修正
  // ここまで修正
  return output;
}
export default conv;
